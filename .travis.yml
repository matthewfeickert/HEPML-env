# test matrix
language: python
cache: pip
python:
  - '3.6'
install:
    - pip install --upgrade --quiet pip
    - pip install --quiet pipenv
    - pipenv install
script:
    - pip freeze

stages:
  - test
